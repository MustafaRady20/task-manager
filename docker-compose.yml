services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: changeme
      MYSQL_DATABASE: taskdb
    ports:
      - "3309:3306"
    volumes:
      - mysql_data:/var/lib/mysql
volumes:
  mysql_data: